---
import Layout from "../../layouts/Layout.astro";
import AdminNav from "../../components/admin/AdminNav.astro";
import AdminReclamos from "../../components/admin/AdminReclamos";
---

<Layout title="Admin · Reclamos">
  <section class="flex flex-col gap-8 px-4 py-12 sm:flex-row sm:py-16">
    <AdminNav activo="reclamos" />
    <div class="min-w-0 max-w-3xl flex-1">
      <h1 class="text-3xl font-extrabold tracking-tight text-neutral-900 dark:text-white sm:text-4xl">
        Reclamos
      </h1>
      <p class="mt-2 text-sm text-neutral-500 dark:text-neutral-400">
        Reclamos y quejas enviados desde el Libro de Reclamaciones Virtual del sitio.
      </p>
      <div class="mt-8">
        <AdminReclamos client:only="preact" />
      </div>
    </div>
  </section>
</Layout>
